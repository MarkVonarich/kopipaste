{
  "name": "finuchet-miniapp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "typecheck": "tsc --noEmit",
    "lint": "node scripts/lint.mjs",
    "test": "vitest run",
    "build": "tsc --noEmit && vite build"
  },
  "devDependencies": {
    "@types/estree": "^1.0.9",
    "@vitejs/plugin-legacy": "^8.2.2",
    "jsdom": "25.0.1",
    "typescript": "5.6.3",
    "vite": "^8.2.0",
    "vitest": "2.1.4"
  }
}
